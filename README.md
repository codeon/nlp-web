# Setting Up

```
$ mkdir api-service
$ cd api-service
$ python3 -m venv venv
$ source venv/bin/activate
$ pip install django
$ pip install spacy
$ django-admin startproject demoapp
$ python manage.py startapp nlp-web
```
